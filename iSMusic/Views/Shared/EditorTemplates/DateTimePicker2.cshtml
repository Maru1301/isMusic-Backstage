@model DateTime?
@{
    var shortDate = DateTime.Parse(DateTime.Now.ToString()).ToString("yyyy-MM-dd HH:mm:ss");
}
@Html.TextBoxFor(m => m, new { @class = "form-control", @type = "datetime", @Value = shortDate , @readonly = "readonly" })



