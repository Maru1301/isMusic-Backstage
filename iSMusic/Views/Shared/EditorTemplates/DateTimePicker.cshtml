@model DateTime?
@{
    var shortDate = DateTime.Parse(DateTime.Now.ToString()).ToString("yyyy-MM-dd HH:mm");

}
@Html.TextBoxFor(m => m, new { @class = "form-control", @type = "datetime-local", @Value = shortDate })



