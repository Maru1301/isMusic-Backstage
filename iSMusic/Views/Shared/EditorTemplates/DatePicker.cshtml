@model DateTime?
@{
    var shortDate = DateTime.Parse(DateTime.Now.ToString()).ToString("yyyy-MM-dd");
}
@Html.TextBoxFor(m => m, new { @class = "form-control", @type = "date", @Value = shortDate})



